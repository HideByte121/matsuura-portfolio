# ポートフォリオのデザイン・改修方針

更新日：2026-09-21

## 1. 目的と基準

HAL東京でゲーム制作を学ぶ学生として、自己紹介・使用技術・作品・連絡先を読みやすく伝える。

基準は現在公開している白・グレーのサイト。現在のHTML・CSS・JavaScriptを活かして段階的に改修する。黒背景・黄緑アクセントの旧デザインには戻さない。

この文書は改修の実装仕様。自己紹介更新・装飾調整・モーダルへの変更はローカル実装へ反映済み。公開サイトへの反映状況は別途確認する。

## 2. 掲載内容（ユーザー確認済み）

### About me

掲載する自己紹介文：

> HAL東京でゲーム制作を学んでいます。シェーダーの制作に興味があり、ゲームの見た目をつくる表現について学んでいきたいと考えています。

| 項目 | 掲載内容 |
| --- | --- |
| 学校 | HAL東京 |
| 使用言語 | C / C++ |
| 使用ツール | Maya / Unity / Unreal Engine |
| 興味のある分野 | シェーダー制作 |
| 氏名 | 未確定。現状の仮表示を維持し、正式な表記は後から設定 |
| 学科・学年 | 未確定。現状の仮表示を維持し、後から設定 |

使用言語と使用ツールは別の項目に分ける。習熟度・経験年数・制作実績は推測して追加しない。

### Works

- 5GameBowling、RE：TRACE、暖弾Doneを掲載する。紹介文は本人の説明と動画概要に基づく。
- カードには画像、作品名、短い説明、使用技術、「詳細を見る」を掲載する。
- 詳細には画像、概要、制作期間、制作人数、担当、使用技術、工夫した点、動画・GitHubなどのリンクを掲載する。
- 架空の作品や実績を作らない。画像未設定・読み込み失敗時は「画像準備中」、リンク未設定時は非表示とする。

### Contact

- 公開メールアドレス：`matsuura.hideyuki1111001@gmail.com`
- アドレスと「メールする」リンクを表示し、閲覧者のメールアプリを開く。
- 問い合わせフォームや送信サーバーは追加しない。

## 3. 見た目

### 合意した方向性

白・グレーを基調にしたシンプルな構成を維持する。ただし、現在はやや淡泊なため、読みやすさを保ちながら少しだけ色と見出しの強弱を加える。作品画像と文章を主役にする。

### 実装時の初期案

以下の色や寸法はユーザー指定ではなく、「少しだけ変化をつける」ための初期案。実画面を見て調整できるようCSS変数で管理する。

| 用途 | 初期値・方針 |
| --- | --- |
| 背景 | 白 `#ffffff` |
| 本文 | 現在の `#242424` |
| 補足文 | 現在の `#666666` |
| 罫線 | 現在の `#dddddd` |
| アクセント | 落ち着いた青 `#365f7d` |
| 薄い背景色 | `#f4f7f9` |
| フォント | 現在のシステムフォントを維持 |
| 本文サイズ・行間 | 現在の15px・1.85を基準に維持 |
| コンテンツ幅 | 現在の最大960pxを維持 |

- 見出しの小さな線、リンク、「メールする」にアクセント色を使用する。
- 言語・ツールを薄い背景の小さなラベルで表示する。熟練度のグラフは設けない。
- 作品カードは画像比率16:9、同じ幅、細い枠線、角丸6px程度に揃える。
- カードのホバーは枠線の色変更程度。大きな画像拡大、背景動画、装飾用の3D表現は設けない。
- 画面上部を巨大なビジュアルで占有せず、名前と短い紹介から本文につなぐ。
- PCは作品2列、600px以下は1列。スマートフォンでもナビゲーションと連絡先を省略しない。

## 4. 作品詳細の操作

今回の選択を優先し、一覧からの詳細表示を別ページ遷移からモーダル（一覧の上に重ねる表示）へ変更する。

- カードを選択すると、同じ画面内のネイティブ`<dialog>`に対応する作品を表示する。
- 背景を半透明の暗色で覆い、詳細領域は白背景にする。
- PCでは最大幅860px程度、スマートフォンでは画面幅から左右16pxずつ余白を取る。
- 高さは画面内に収め、長い内容はモーダル内でスクロールする。画像は縦に並べる。
- 「閉じる」ボタン、Escキー、背景部分のクリックで閉じられる。詳細内のクリックでは閉じない。
- 開いたらフォーカスをモーダル内へ移し、閉じたら選択元のカードへ戻す。
- 開いている間は背面のスクロールを止める。見出しとモーダルをアクセシビリティ属性で関連付ける。
- 既存の`work.html?id=...`は直接アクセス用に残す。既存URLを壊さず、作品データと詳細描画処理を共有する。
- モーダルの開閉では新たにURLやブラウザー履歴を変更しない。

## 5. 既存コードの活用と実装順

現在の静的サイト構成を維持し、新しいフレームワーク・ビルド工程・外部UIライブラリーは追加しない。

1. 既存トップページの自己紹介とプロフィール欄を、上記の確定情報へ更新する。
2. `css/styles.css`の既存レイアウトを維持し、アクセント色・ラベル・カードの装飾を小さく追加する。
3. `js/scripts.js`のカード生成・画像の代替表示・メールリンク・外部リンク処理を活かし、詳細描画を共通化してモーダル表示を追加する。
4. `js/data.js`を作品と連絡先の共通データとして引き続き使う。作品情報を追加する際もここを編集する。
5. READMEの詳細表示に関する説明を実装に合わせて更新する。
6. ローカルで表示・操作を確認する。文書作成だけでは公開せず、サイトの実装・公開状況は分けて報告する。

## 6. 完了の確認項目

- 白・グレーの既存サイトの構成が保たれ、控えめなアクセントが追加されている。
- 自己紹介、C・C++、Maya・Unity・Unreal Engine、シェーダーへの関心が表示される。
- 作品が未登録でも仮データであることが分かり、画像・リンクの欠落で崩れない。
- 各カードから正しい作品のモーダルが開き、閉じるボタン・Esc・背景クリックで閉じられる。
- TabキーとEnterキーで作品を開け、閉じた後は元のカードにフォーカスが戻る。
- モーダルの再開閉でスクロール制限が残らない。
- 既存の詳細URLへの直接アクセスと不明なIDへの案内が機能する。
- PCと幅375px・320pxの画面で横にはみ出さず、長い詳細を最後まで読める。
- メールリンクの宛先が指定アドレスと一致する。

## 7. YouTube動画の追加仕様

- 作品データの `youtubeId` を使って詳細の先頭に埋め込みプレイヤーを表示する。自動再生はしない。
- 動画なしの場合は既存の画像表示を利用する。動画ありの場合も追加画像を縦に並べられる。
- モーダルを閉じたらiframeを削除し、再生を停止する。
- 一覧画像が未指定の場合は動画サムネイルを利用する。
- 埋め込み再生が利用できない環境向けに「YouTubeで見る」を併記する。
- 既存IDと詳細URLを維持し、一覧と詳細ページで同じ内容を表示する。
